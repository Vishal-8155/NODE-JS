<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crud</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
</head>

<body>

    <div class="container-fluid p-0">
        <!-- Small boxes (Stat box) -->
        <div class="row" style="height: 100vh;">

            <div class="col-12 col-lg-4 bg-secondary-subtle p-5 pt-4">

                <b class="pb-4"
                    style="width: 100%; display: flex; justify-content: center; align-items: center; font-size: 35px; text-decoration: underline;">MOVIE CRUD OPERATION</b>

                <%let a='' %>
                    <% for(let i in userdata2){ %>
                        <% a=userdata2[i] %>
                            <%} %>
                                <form action="/savedata" method="post" name="frm" enctype="multipart/form-data">

                                    <input type="hidden" name="hid" value="<%= a._id  %>">

                                    Movie Name:
                                    <input type="text" name="name" placeholder="Username is reqire"
                                        value="<%= a.moviename %>" class="my-2"
                                        style="width: 100%;"><br><br>

                                    Image:
                                    <input type="file" name="image"><br><br>

                                    <input type="submit" value="Submit" class="btn btn-dark w-100" style="background-color: #3f454b;"
                                    style="margin-top: -20px; cursor: pointer;">


                                </form>
                                <br><br>

            </div>

            <div class="col-12 col-lg-8 bg-secondary d-flex justify-content-center align-items-start"
                style="padding-top: 35px;">

                <table class="table table-striped" border="3" cellpadding="8">
                    <tr>

                        <td>id</td>
                        <td>Name</td>
                        <td>Image</td>
                        <td colspan="2">Action</td>

                    </tr>
                    <% for(let i in data){ %>
                        <tr>
                            <td>
                                <%= data[i]._id %>
                            </td>
                            <td>
                                <%= data[i].moviename %>
                            </td>

                            <td><img src="../<%= data[i].pimage %>" height="100px" width="200px"></td>
                            <td><a href="/edit/<%= data[i]._id%>" style="text-decoration: none;">Edit</a></td>
                            <td><a href="/del/<%= data[i]._id%>" style="text-decoration: none;">Delete</a></td>
                        </tr>
                        <%} %>
                </table>

            </div>

        </div>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"
        crossorigin="anonymous"></script>
</body>

</html>