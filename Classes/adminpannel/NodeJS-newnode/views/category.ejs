

<%- include ('./partials/header')%>
<%- include ('./partials/sidebar',{username:username})%>
<%- include ('./partials/navbar',{username:username})%>

            <!-- Form Start -->
            <div class="container-fluid pt-4 px-4">
                <% if(message2){ %>
                    <p class="w-100 rounded p-2 mb-3 btn-primary text-white" role="alert">
                        <%= message2 %>
                    </p>
                <% } %>
                <div class="row g-4">
                    <div class="col-sm-12 col-xl-6">
                        <div class="bg-secondary rounded h-100 p-4">
                            <h6 class="mb-4">Category</h6>
                            <form action="<%= (editcat)?'/admin/updatecategory/'+editcat._id:'/admin/savecategory'%>" method="post" name='frm'>
                                <div class="mb-3">
                                    <label for="exampleInputEmail1" class="form-label">Category Name</label>
                                    <input type="text" name="catname" class="form-control" value="<%= (editcat)? editcat.catname: '' %>">
                                </div>
                                <button type="submit" class="btn btn-primary">Save</button>
                            </form>
                        </div>
                    </div>
                   
                    <div class="col-sm-12 col-xl-6">
                        <div class="bg-secondary rounded h-100 p-4">
                            <h6 class="mb-4">All Categories</h6>
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th scope="col">#</th>
                                        <th scope="col">Category Name</th>
                                        <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                 
                                <tbody>
                                    <% if(getAllCat) { %>
                                        <% getAllCat.forEach((item)=>{ %>
                                            <tr>
                                                <td><%= item.id %></td>
                                                <td><%= item.catname %></td>
                                                <td><a href="/admin/editcat/<%= item._id%>">Edit</a> <a href="/admin/deletecat/<%= item._id%>">Delete</a></td>
                                            </tr>
                                        <% }) %>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Form End -->


<%- include ('./partials/footer')%>

         